customer_ID = ''                # int
first_Name = 'Jeanette'         # str
last_Name = 'Her'               # str
gender = 'F'                    # str
customer_DOB = '1980-05-12'     # str
dl_number = 'D1234567'          # str
policy_Number = 'PA-987654'     # str

my_name = "Fuat Berk Baslar"
birth_city_state = "Erzincan, Türkiye"

# a) What is the full list of reserved words that can’t be used for variable names?
# False	        def	        if	            raise
# None	        del	        import	        return
# True	        elif	    in	            try
# and	        else	    is	            while
# as	        except	    lambda	        with
# assert	    finally	    nonlocal	    yield
# break	        for	        not
# class	        form	    or
# continue	    global	    pass

# b) Pick 5 of these words and review the explanation for how it is used as a keyword in
#   Python. Add these 5 definitions as # comments to your exercise document. Put ^^
#   around any terms that you are not familiar with.

# def = used to define a function in Python. 
# class = used to create a class. 
# continue = used inside loops to skip the rest of the current iteration and continue with the next
# while = used to repeat a block while a condition is True 
# import = used to bring code from modules or libraries into the current file 
